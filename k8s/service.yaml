apiVersion: v1
kind: Service
metadata:
  name: telegram-bot
  namespace: telegram-bot
spec:
  selector:
    app: telegram-bot
  ports:
    - protocol: TCP
      port: 8443
      targetPort: 8443
  type: ClusterIP
